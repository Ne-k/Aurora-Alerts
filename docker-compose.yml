services:
  noaa_alert:
    build: .
    volumes:
      - .:/app
    environment:
      - DISCORD_WEBHOOK=${DISCORD_WEBHOOK}
      - KP_THRESHOLD=${KP_THRESHOLD:-6.5}
    command: ["/app/start.sh"]
    restart: always
  